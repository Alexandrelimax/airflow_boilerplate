config {
  type: "assertion",
  tags: ["assertion", "orders", "status"],
  assertions: {
    rowConditions: [
      "status IN (${constants.validOrderStatus.map(s => `'${s}'`).join(', ')})"
    ]
  }
}

SELECT * FROM ${ref("orders_enriched")}
