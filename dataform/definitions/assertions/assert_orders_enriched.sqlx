config {
  type: "assertion",
  assertions: {
    uniqueKey: ["order_id"],
    nonNull: ["order_id", "user_id", "order_date", "product_id"],
    rowConditions: [
      "quantity > 0",
      "item_price >= 0"
    ]
  }
}

SELECT * FROM ${ref("orders_enriched")}
